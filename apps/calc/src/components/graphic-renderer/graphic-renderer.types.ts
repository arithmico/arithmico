export type RenderTarget = "web" | "pdf";

export type GraphicDimensions = {
  width: number;
  height: number;
};

export type GraphicLimits = {
  xMin: number;
  yMin: number;
  xMax: number;
  yMax: number;
};

export type Ticks = {
  xTicks: number;
  yTicks: number;
};
